\documentclass[11pt,letterpaper]{article}

\usepackage[spanish,es-nodecimaldot]{babel}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lmodern}
\usepackage[margin=2.4cm]{geometry}
\usepackage{amsmath,amssymb}
\usepackage{booktabs,longtable,tabularx,array}
\usepackage{graphicx,float}
\usepackage{pdflscape}
\graphicspath{{assets/}{docs/informe/assets/}}
\usepackage{xcolor}
\usepackage{enumitem}
\usepackage[hidelinks]{hyperref}

\newcommand{\MinPol}{{\normalfont\scshape MinPol}}
\newcommand{\pendiente}[1]{\textcolor{red!70!black}{\textbf{PENDIENTE: #1}}}
\setlength{\parskip}{0.55em}
\setlength{\parindent}{0pt}

\begin{document}

\begin{titlepage}
\centering

{\Large\scshape Universidad del Valle\par}
\vspace{0.25cm}
{\large Facultad de Ingeniería\par}
\vspace{0.1cm}
{\large Escuela de Ingeniería de Sistemas y Computación\par}
\vspace{0.1cm}
{\large Análisis de Algoritmos II\par}

\vspace{1.35cm}

{\LARGE\bfseries Informe de Proyecto\par}
\vspace{0.35cm}
{\large Proyecto de curso\par}

\vspace{1.1cm}

{\LARGE\bfseries
El Problema de Minimizar la Polarización\\
presente en una Población: \MinPol\par}

\vspace{0.7cm}
\rule{0.82\textwidth}{0.5pt}
\vspace{1.0cm}

\begin{minipage}{0.84\textwidth}
\begin{tabularx}{\textwidth}{@{}>{\bfseries}p{2.8cm}X@{}}
Estudiantes: &
Juan Carlos Cruz Muñoz (1824389), David\newline
Alejandro Enciso Gutierrez (2240581), Juan\newline
Esteban Rodriguez Valencia (2042282),\newline
Estiven Andres Martinez Granados (2179687)\\[0.65em]
Profesor: & Jesús Alexander Aranda\\[0.25em]
Monitor: & Samuel Galindo
\end{tabularx}
\end{minipage}

\vfill

{\large Cali, Colombia\par}
\vspace{0.15cm}
{\large Julio de 2026\par}
\end{titlepage}

\tableofcontents
\newpage

\section{Introducción}

La polarización de una población puede interpretarse como el esfuerzo
necesario para aproximar sus opiniones a un punto central. En este proyecto
se estudia el problema \MinPol{}: dada una distribución inicial de personas
entre opiniones numéricas y un presupuesto limitado para modificar dichas
opiniones, además de un límite de movimientos, se deben seleccionar
movimientos que minimicen la suma de distancias respecto de la mediana
ponderada final.

Se construyó un modelo de programación lineal entera (PLE), no mixto ni
no lineal: todas las variables de decisión son enteras o binarias y los
productos entre variables se linealizan. El modelo se implementó en MiniZinc
y se integró con una interfaz gráfica desarrollada en Python y Tkinter. La
aplicación valida archivos de entrada, conserva exactamente los datos
decimales, genera el archivo \texttt{DatosProyecto.dzn}, ejecuta el solver y
presenta la matriz de movimientos, la distribución final, el costo, la
cantidad de movimientos usados, la polarización y la mediana seleccionada.

El código fuente, las instancias y los artefactos reproducibles del proyecto
se encuentran en el repositorio
\href{https://github.com/Juan-Carlos-Cruz/Proyecto-II-ADA-II}
{\texttt{github.com/Juan-Carlos-Cruz/Proyecto-II-ADA-II}}.

\section{Descripción del problema}

Sea \(n\) el número de personas y \(m\) el número de opiniones posibles. La
distribución inicial está dada por \(p_i\), mientras que \(v_i\in[0,1]\)
representa el valor de la opinión \(i\). Mover una persona desde \(i\) hasta
\(j\) tiene costo base \(c_{i,j}\). Cuando la opinión de destino estaba vacía
inicialmente, es decir \(p_j=0\), se suma el costo extra \(ce_j\).

El costo de mover \(x_{i,j}\) personas es:

\[
x_{i,j}\left[
c_{i,j}\left(1+\frac{p_i}{n}\right)
+\mathbf{1}_{p_j=0}ce_j
\right].
\]

El factor \(1+p_i/n\) hace que resulte más costoso intervenir grupos de
origen grandes. El costo extra depende exclusivamente de la distribución
inicial, no de si la opinión se ocupa posteriormente.

Además, mover una persona de la opinión \(i\) a la opinión \(j\) consume
\(|j-i|\) movimientos. La suma de esos movimientos no puede superar el
parámetro \(maxM\).

Si \(y_i\) es la cantidad final de personas con opinión \(i\), la
polarización se define como:

\[
\operatorname{Pol}(y,v)=
\sum_{i=1}^{m}y_i\left|v_i-\operatorname{mediana}(y,v)\right|.
\]

\subsection{Ejemplo del enunciado}

El ejemplo tiene \(n=20\), \(m=5\), \(p=(12,4,0,4,0)\),
\(v=(0,0.25,0.5,0.75,1)\), \(ct=20\) y \(maxM=18\). Tres salidas
factibles son:

\begin{center}
\begin{tabular}{p{4.2cm}rrrp{3.6cm}}
\toprule
Movimientos & Costo & Movs. & Polarización & Distribución final\\
\midrule
Ninguno & 0 & 0 & 4 & \([12,4,0,4,0]\)\\
4 personas de 4 a 2 & 9.6 & 8 & 2 & \([12,8,0,0,0]\)\\
4 de 2 a 1; 2 de 4 a 1; 2 de 4 a 2 & 19.2 & 14 & 0.5 &
\([18,2,0,0,0]\)\\
\bottomrule
\end{tabular}
\end{center}

La tercera salida es óptima. MiniZinc certificó el valor 0.5 y una
enumeración independiente comprobó las 447 distribuciones finales distintas
alcanzables bajo presupuesto y límite de movimientos. Si el presupuesto
aumenta a 24, se puede alcanzar consenso en la opinión 1 usando 16
movimientos, todavía dentro de \(maxM=18\).

\section{Modelo matemático}

La formulación se presenta mediante cinco componentes: conjuntos e índices,
parámetros, variables de decisión, restricciones y función objetivo.

\subsection{Conjuntos y parámetros}

Sea \(O=\{1,\ldots,m\}\). Para \(i,j,k\in O\):

\begin{itemize}[nosep]
    \item \(n\in\mathbb{N}\): población total.
    \item \(p_i\in\{0,\ldots,n\}\): población inicial en la opinión \(i\),
    con \(\sum_i p_i=n\).
    \item \(v_i\in[0,1]\): valor de la opinión.
    \item \(c_{i,j}\ge0\): costo base por movimiento.
    \item \(ce_j\ge0\): costo extra del destino inicialmente vacío.
    \item \(ct\ge0\): presupuesto.
    \item \(maxM\in\mathbb{N}^{+}\): máximo número de movimientos.
\end{itemize}

Se define el parámetro binario \(e_j=1\) si \(p_j=0\), y \(e_j=0\) en caso
contrario. El costo unitario efectivo es:

\[
a_{i,j}=c_{i,j}\left(1+\frac{p_i}{n}\right)+e_jce_j.
\]

\subsection{Variables}

\begin{itemize}[nosep]
    \item \(x_{i,j}\in\{0,\ldots,n\}\): personas que pasan de \(i\) a \(j\).
    \item \(y_i\in\{0,\ldots,n\}\): población final en \(i\).
    \item \(z_k\in\{0,1\}\): selección de \(v_k\) como mediana.
    \item \(q_{i,k}\in\{0,\ldots,n\}\): linealización de \(y_i z_k\).
\end{itemize}

\subsection{Restricciones}

No se modela como movimiento permanecer en la misma opinión:

\[
x_{i,i}=0 \qquad \forall i\in O.
\]

No pueden salir más personas de las existentes inicialmente:

\[
\sum_{\substack{j\in O\\j\ne i}}x_{i,j}\le p_i
\qquad \forall i\in O.
\]

La distribución final se obtiene mediante balance:

\[
y_i=p_i-\sum_{\substack{j\in O\\j\ne i}}x_{i,j}
+\sum_{\substack{j\in O\\j\ne i}}x_{j,i}
\qquad \forall i\in O,
\]

\[
\sum_{i\in O}y_i=n.
\]

La restricción presupuestaria es:

\[
\sum_{i\in O}\sum_{\substack{j\in O\\j\ne i}}
a_{i,j}x_{i,j}\le ct.
\]

La restricción de movimientos es:

\[
\sum_{i\in O}\sum_{\substack{j\in O\\j\ne i}}
|j-i|x_{i,j}\le maxM.
\]

Para elegir exactamente una mediana:

\[
\sum_{k\in O}z_k=1.
\]

El producto \(q_{i,k}=y_i z_k\) se linealiza con:

\[
\begin{aligned}
q_{i,k}&\le y_i,\\
q_{i,k}&\le nz_k,\\
q_{i,k}&\ge y_i-n(1-z_k),\\
q_{i,k}&\ge0.
\end{aligned}
\qquad \forall i,k\in O.
\]

\subsection{Función objetivo}

\[
\min \quad
P=\sum_{i\in O}\sum_{k\in O}|v_i-v_k|q_{i,k}.
\]

Para una distribución final fija, el valor que minimiza la suma de
desviaciones absolutas es una mediana ponderada. Por eso la minimización
conjunta sobre \(x\) y \(z\) selecciona automáticamente una mediana válida.
En una población par puede existir un intervalo de medianas; alguno de sus
extremos coincide con un \(v_k\) y conserva el mismo valor objetivo.

\section{Justificación del modelo}

Cada matriz entera \(x\) representa directamente los esfuerzos solicitados.
La restricción de salida evita reutilizar personas que llegaron desde otra
opinión y garantiza que cada individuo se mueva a lo sumo una vez. Las
ecuaciones de balance conservan la población. La restricción de presupuesto
reproduce literalmente la fórmula del enunciado, incluido el costo extra por
persona para destinos inicialmente vacíos. La restricción de movimientos
reproduce el conteo \(|j-i|\) exigido para cada persona movida.

La linealización de la mediana es exacta porque \(0\le y_i\le n\). Si
\(z_k=0\), las restricciones fuerzan \(q_{i,k}=0\); si \(z_k=1\), fuerzan
\(q_{i,k}=y_i\). Por tanto, el objetivo es exactamente la polarización
respecto del valor seleccionado.

En consecuencia, toda solución de \MinPol{} puede representarse en el modelo,
y toda solución óptima del modelo proporciona movimientos válidos con la
menor polarización posible.

\section{Detalles importantes de implementación}

\subsection{Escalamiento entero}

Los archivos MPL admiten decimales, pero el modelo utiliza enteros escalados
para evitar errores de representación binaria. Si \(S_v\) y \(S_c\) son
potencias de diez apropiadas:

\[
V_i=S_vv_i,\quad C_{i,j}=S_cc_{i,j},\quad
CE_j=S_cce_j,\quad CT=S_cct.
\]

El costo unitario se multiplica además por \(n\):

\[
U_{i,j}=C_{i,j}(n+p_i)+e_jCE_jn.
\]

MiniZinc impone

\[
\sum_{i,j:i\ne j}U_{i,j}x_{i,j}\le CTn.
\]

Esta transformación es algebraicamente equivalente al costo original. El
conversor usa el tipo \texttt{Decimal} de Python y calcula automáticamente
las escalas mínimas necesarias.

\subsection{Conversión y validación}

El módulo \texttt{mpl\_parser.py} verifica:

\begin{itemize}[nosep]
    \item presencia de las \(7+m\) líneas requeridas;
    \item positividad de \(n\) y \(m\);
    \item dimensiones de todos los vectores y de la matriz;
    \item valores enteros no negativos en \(p\) y \(\sum_i p_i=n\);
    \item \(0\le v_i\le1\);
    \item costos y presupuesto no negativos y finitos;
    \item \(maxM\) entero positivo.
\end{itemize}

Después genera \texttt{DatosProyecto.dzn}. La validación sigue el orden
actualizado de la sección 3.1: \(n\), \(m\), \(p\), \(v\), \(ce\), matriz
\(c\), \(ct\) y \(MaxMovs\).

\subsection{Interfaz gráfica}

La interfaz utiliza Tkinter y permite cargar un MPL, inspeccionar los datos,
generar el DZN y ejecutar \texttt{Proyecto.mzn}. La ejecución se realiza en
un hilo separado para no bloquear la ventana. Los resultados se presentan
en formato resumido y también se conserva la salida completa con
estadísticas. El usuario puede elegir Chuffed, HiGHS o COIN-BC.
Además, la vista de resultados recalcula y muestra siete comprobaciones:
dominio entero no negativo, ausencia de automovimientos, disponibilidad por
origen, balance y conservación de población, presupuesto, límite de
movimientos y consistencia de la mediana con la polarización. Así, el
cumplimiento de las restricciones
puede inspeccionarse directamente sin interpretar la salida técnica.

\subsection{Portabilidad y decisión sobre Docker}

La aplicación usa únicamente la biblioteca estándar de Python para su
interfaz y lógica de ejecución; MiniZinc es la única dependencia externa de
producción. El entorno se comprueba con:

\begin{center}
\texttt{python3 }\path{ProyectoGUIFuentes/verificar_entorno.py}
\end{center}

Este comando revisa Python, Tkinter, MiniZinc y los solvers compatibles. El
\texttt{Readme.txt} incluye instrucciones para Windows, macOS y Linux y
permite indicar una instalación no estándar mediante
\texttt{MINIZINC\_EXECUTABLE}.

Se evaluó Docker, pero no se adoptó como mecanismo principal: Tkinter requiere
acceso al sistema gráfico y a los diálogos nativos. Exponer una GUI desde un
contenedor requiere configuraciones diferentes para X11, Wayland y Docker
Desktop. Un contenedor sería útil para pruebas automáticas sin interfaz, pero
para el entregable gráfico la ejecución nativa es más directa y portable.

\section{Análisis de Branch and Bound}

Branch and Bound mantiene una solución entera incumbente que actúa como cota
superior en un problema de minimización. La relajación de cada nodo
proporciona una cota inferior. Un nodo se poda cuando es infactible, cuando
su cota no puede mejorar al incumbente o cuando produce una solución entera.
La optimalidad se demuestra cuando no quedan nodos cuya cota pueda mejorar
la mejor solución.

\subsection{Ejemplo pequeño explicado paso a paso}

Se construyó la instancia reproducible
\texttt{DatosProyecto/branch\_and\_bound/ejemplo\_pequeno.mpl}, con
\(n=4\), \(m=3\), \(p=(2,0,2)\), \(v=(0,0.5,1)\), \(ce=(0,0,0)\),
\(ct=3.5\), \(maxM=4\) y

\[
C=\begin{pmatrix}
0&1&10\\
10&0&10\\
10&2&0
\end{pmatrix}.
\]

Los únicos arcos no diagonales cuyo costo cabe en el presupuesto son
\(1\to2\) y \(3\to2\). Sean \(a=x_{1,2}\) y \(b=x_{3,2}\). El costo efectivo
de cada traslado por el primer arco es
\(1(1+2/4)=3/2\), mientras que por el segundo es
\(2(1+2/4)=3\). La distribución resultante es
\([2-a,a+b,2-b]\). La opinión central, de valor 0.5, siempre es una mediana
óptima para estas soluciones, por lo que la instancia se reduce a:

\[
\begin{aligned}
\min\quad & P=2-\tfrac{1}{2}(a+b)\\
\text{sujeto a}\quad
& \tfrac{3}{2}a+3b\le \tfrac{7}{2},\\
&0\le a\le2,\quad 0\le b\le2,\\
&a,b\in\mathbb{Z}.
\end{aligned}
\]

En el nodo raíz se omite temporalmente la integralidad. La relajación lineal
usa primero el traslado más barato y obtiene
\((a,b)=(2,1/6)\), con cota inferior
\(LB=2-\tfrac12(2+1/6)=11/12\). Como \(b\) es fraccionaria, Branch and Bound
crea los dos subproblemas \(b\le\lfloor1/6\rfloor=0\) y
\(b\ge\lceil1/6\rceil=1\).

En la rama \(b\le0\), la relajación produce \((a,b)=(2,0)\), una solución
entera con polarización 1. Esta solución se convierte en el incumbente y
establece \(UB=1\). En la rama \(b\ge1\), la mejor solución relajada es
\((a,b)=(1/3,1)\), con \(LB=4/3\). Como es un problema de minimización y
\(4/3\ge1\), toda solución entera descendiente de esa rama es incapaz de
mejorar el incumbente; por tanto, se poda por cota.

\begin{center}
\begin{tabular}{llll}
\toprule
Nodo & Restricción añadida & Óptimo de la relajación & Decisión\\
\midrule
Raíz & ninguna & \((2,1/6),\ LB=11/12\) & ramificar en \(b\)\\
Izquierdo & \(b\le0\) & \((2,0),\ P=1\) & entero; fijar \(UB=1\)\\
Derecho & \(b\ge1\) & \((1/3,1),\ LB=4/3\) & podar: \(LB\ge UB\)\\
\bottomrule
\end{tabular}
\end{center}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{arbol_bb_pequeno.pdf}
\caption{Árbol de Branch and Bound de la instancia pequeña: ramificación
sobre una variable fraccionaria y poda por cota.}
\end{figure}

Al cerrar las dos ramas no quedan nodos abiertos y se certifica que
\((a,b)=(2,0)\) es óptima. En términos de \MinPol{}, se trasladan dos personas
de la opinión 1 a la 2, con costo 3, dos movimientos, distribución final
\([0,2,2]\), mediana seleccionada 0.5 y polarización 1. MiniZinc y HiGHS
confirman ese óptimo con objetivo escalado 10. El solver industrial puede
cerrar la instancia en el nodo raíz mediante preprocesamiento y cortes; el
árbol anterior expone deliberadamente el procedimiento clásico sobre la
relajación lineal para hacer visibles la ramificación y la poda.

\subsection{Ejemplo de mayor búsqueda}

En el ejemplo del enunciado, HiGHS resolvió el modelo en el nodo raíz: el
preprocesamiento, la relajación y los cortes fueron suficientes para obtener
y certificar el objetivo escalado 50. COIN-BC reportó también cero nodos
adicionales. Chuffed mostró una búsqueda discreta más extensa:

\begin{center}
\begin{tabular}{lr}
\toprule
Indicador & Valor\\
\midrule
Nodos & 225\\
Fallos & 114\\
Reinicios & 18\\
Profundidad máxima & 9\\
Propagaciones & 17\,198\\
Backjumps & 11\\
Objetivo escalado & 50\\
\bottomrule
\end{tabular}
\end{center}

Después de encontrar 50, Chuffed añadió implícitamente la exigencia de
buscar una solución con objetivo menor. La salida final indica que la
búsqueda con límite 49 no tiene solución, lo cual prueba el óptimo. Los
árboles cambian entre solvers porque cada uno usa presolve, variables de
ramificación, cortes, aprendizaje y reinicios diferentes. Por ello el número
de nodos no debe compararse sin identificar solver, versión y configuración.

Para el modelo actualizado con \(maxM\), la ejecución con Gecode 6.3.0
reportó 13 soluciones, 50 nodos, 11 fallos y profundidad máxima 18. Estas
cifras se reproducen desde la raíz del proyecto con
\texttt{minizinc -{}-solver Gecode -{}-statistics Proyecto.mzn}
\texttt{DatosProyecto/ejemplos/ejemplo\_pdf.dzn}.

La figura~\ref{fig:arbol-gist} muestra la ejecución interactiva del mismo
modelo y DZN en Gecode Gist. Después de explorar todas las soluciones, el
visualizador registró profundidad 19 (contando la raíz), 13 nodos solución,
17 nodos fallidos, 29 nodos de ramificación y cero nodos abiertos. El conteo
visual no coincide directamente con las estadísticas del motor porque Gist
clasifica y contabiliza los nodos dibujados, mientras el modo estadístico
reporta eventos internos de búsqueda.

\begin{figure}[H]
\centering
\includegraphics[width=0.98\textwidth]{arbol_minizinc_instancia_mayor.png}
\caption{Árbol completamente explorado en Gecode Gist para el ejemplo del
enunciado con la restricción \(maxM\). La barra inferior confirma cero nodos
abiertos.}
\label{fig:arbol-gist}
\end{figure}

\section{Pruebas realizadas}

La tabla principal se ejecutó con MiniZinc 2.9.7 y HiGHS 1.14.0. La batería
de 30 entradas suministradas se resolvió con COIN-BC 2.10.13, que reproduce
el óptimo de referencia 10.313 de \texttt{MinPol15}. Los tiempos corresponden
al solver y son mediciones puntuales.

\begin{center}
\scriptsize
\begin{tabular}{lrrrrrrrrr}
\toprule
Instancia & \(n\) & \(m\) & \(ct\) & \(maxM\) & Movs. & Costo &
Polarización & Tiempo (s) & Nodos\\
\midrule
Ejemplo PDF & 20 & 5 & 20 & 18 & 14 & 19.2 & 0.5 & 0.0255 & 1\\
Instancia 1 & 10 & 4 & 12 & 6 & 6 & 11.7 & 0 & 0.0121 & 1\\
Instancia 2 & 20 & 5 & 10 & 7 & 7 & 9.8 & 5.8 & 0.1048 & 9\\
Instancia 3 & 24 & 6 & 30 & 18 & 18 & 30 & 6.75 & 0.1867 & 11\\
Instancia 4 & 18 & 6 & 18.75 & 15 & 14 & 18.6667 & 2.75 & 0.2588 & 76\\
Instancia 5 & 40 & 8 & 45 & 25 & 24 & 44.85 & 11.7 & 0.3547 & 15\\
\bottomrule
\end{tabular}
\end{center}

Todos los estados fueron óptimos: HiGHS reportó igualdad entre el objetivo
incumbente y la cota. Además se ejecutaron pruebas unitarias del parser,
el escalamiento, las validaciones, el procesamiento de resultados y la
comprobación independiente de restricciones.

La batería completa se reproduce con
\texttt{python3 Pruebas/ejecutar\_pruebas.py}. Después,
\texttt{python3 Pruebas/verificar\_soluciones.py} recalcula cada solución sin
confiar en las etiquetas impresas por MiniZinc: comprueba dominio, diagonal,
disponibilidad, balance, conservación, costo, presupuesto, límite de
movimientos y polarización mínima respecto de todos los valores candidatos a
mediana.

También se procesaron las 30 entradas de
\texttt{bateria\_pruebas/mpl}:

\begin{center}
\begin{tabular}{lrl}
\toprule
Entradas & Cantidad & Resultado\\
\midrule
\texttt{MinPol1--30} & 30 & Óptimo y verificaciones aprobadas\\
\bottomrule
\end{tabular}
\end{center}

Las 30 entradas fueron convertidas a DZN antes de ejecutar el modelo con
COIN-BC. En \texttt{MinPol28} y \texttt{MinPol29}, \(n=125\) coincide con
\(\sum p_i=125\) y permite reproducir los valores de referencia suministrados.
La tabla completa se encuentra en
\texttt{Pruebas/resultados\_bateria\_suministrada.csv} y se regenera con
\texttt{python3 Pruebas/verificar\_bateria\_suministrada.py}.

Veintitrés valores coinciden textualmente con la tabla de referencia. Los
siete restantes difieren únicamente en 0.001: el modelo entero escalado
obtiene 9.687, 6.550, 14.172, 17.898, 23.567, 24.176 y 30.000. Esta diferencia
es compatible con truncamiento de representaciones de punto flotante en los
valores publicados.

\section{Análisis de resultados}

\subsection[Variación de m]{Variación de \(m\)}

El modelo tiene dos familias principales de \(m^2\) variables: movimientos
\(x\) y productos auxiliares \(q\). Su tamaño crece cuadráticamente. Sin
embargo, los nodos no crecen de manera monótona: la instancia 4 con seis
opiniones necesitó 76 nodos, mientras la instancia 5 con ocho necesitó 15.
La simetría, los costos, la holgura y la cantidad de soluciones cercanas al
óptimo también determinan la dificultad.

\begin{figure}[H]
\centering
\includegraphics[width=0.82\textwidth]{nodos_instancias.png}
\caption{Nodos de Branch and Bound en las cinco instancias.}
\end{figure}

\subsection{Variación del presupuesto}

En el ejemplo, la polarización óptima pasa de 4 con \(ct=0\), a 2 con
\(ct=10\), 0.5 con \(ct=20\) y 0 con \(ct=24\). Aumentar el presupuesto no
puede empeorar el óptimo porque amplía el conjunto factible. La mejora puede
ser escalonada porque las personas son indivisibles. En esta comparación se
mantiene \(maxM=18\), por lo que el cambio observado corresponde al
presupuesto.

\begin{figure}[H]
\centering
\includegraphics[width=0.82\textwidth]{presupuesto_polarizacion.png}
\caption{Presupuesto frente a polarización óptima.}
\end{figure}

\subsection{Consenso y costos extra}

La instancia 1 alcanza consenso con costo 11.7. El ejemplo requiere
presupuesto 24 para lograrlo. En las demás configuraciones, concentrar toda
la población en una opinión excede el presupuesto o el límite de
movimientos.

La instancia 3 obtuvo polarización 6.75 cuando los destinos centrales vacíos
tenían costo extra 12. Al fijar \(ce=0\), la polarización cayó a 6. La
mejora es menor que en el modelo sin \(maxM\), porque el límite de
movimientos también restringe el uso de opiniones centrales. El costo extra
puede cambiar tanto los movimientos como la mediana final.

\begin{figure}[H]
\centering
\includegraphics[width=0.72\textwidth]{impacto_costo_extra.png}
\caption{Resultado de la instancia 3 con y sin costo extra.}
\end{figure}

\subsection{Bondades, falencias, eficiencia y optimalidad}

\textbf{Bondades.} La formulación es genérica respecto de \(n\) y \(m\),
preserva exactamente los decimales mediante escalamiento entero y mantiene
lineales tanto el presupuesto como la función objetivo. La linealización
\(q_{i,k}=y_i z_k\) permite seleccionar una mediana ponderada sin depender de
una función no lineal ni de un solver específico.

\textbf{Falencias.} Las familias \(x\) y \(q\) contienen \(m^2\) variables,
y la linealización de \(q\) añade tres desigualdades por par \((i,k)\). Por
ello, memoria y tiempo pueden crecer rápidamente cuando aumenta \(m\).
Además, el escalamiento con muchos decimales produce coeficientes enteros
grandes y puede acercarse a los límites numéricos del backend. El modelo no
rompe todas las simetrías que aparecen cuando varias opiniones tienen el
mismo valor o costo.

\textbf{Eficiencia.} En las seis instancias resumidas en la tabla, HiGHS
tardó entre aproximadamente 0.01 y 0.36 segundos y exploró entre 1 y 76
nodos. En las 30 entradas de la batería suministrada, COIN-BC empleó entre
0.0031 y 5.0107 segundos y exploró entre 0 y 184 nodos, con
\(3\le m\le20\) y \(5\le n\le200\). No se
observa crecimiento
monótono con \(m\): la estructura de costos, la simetría y la holgura del
presupuesto influyen además del tamaño cuadrático del modelo. Las
validaciones previas y el uso de coeficientes enteros evitan enviar al solver
entradas inválidas o aproximadas.

\textbf{Optimalidad.} El modelo no usa una heurística para decidir el valor
final: los solvers completan una búsqueda exacta. En HiGHS, la igualdad entre
objetivo primal y cota dual y una brecha de 0\,\% certifica el óptimo. En
Chuffed, después del incumbente se demuestra que no existe una solución con
objetivo entero escalado estrictamente menor. Estas certificaciones, junto
con las pruebas sobre varias instancias, sustentan la optimalidad de las
soluciones informadas.

\section{Conclusiones}

\begin{enumerate}
    \item El problema \MinPol{} puede representarse exactamente mediante un
    modelo entero lineal al seleccionar la mediana con variables binarias y
    linealizar los productos correspondientes.
    \item El escalamiento entero conserva los decimales de entrada y evita
    depender de tolerancias de punto flotante en la formulación.
    \item El presupuesto controla directamente la reducción posible de la
    polarización; el consenso aparece únicamente cuando existe una
    concentración completa cuyo costo no excede \(ct\) y cuyos movimientos
    no exceden \(maxM\).
    \item Los costos extra penalizan el uso de opiniones inicialmente vacías
    y pueden impedir que las opiniones centrales, aunque cercanas a la
    mediana, formen parte de la solución.
    \item El número de opiniones incrementa el tamaño cuadráticamente, pero
    la dificultad efectiva depende también de la estructura de costos y de
    la simetría.
    \item Las diez pruebas de integración, las ocho pruebas unitarias y las 30
    entradas válidas de la batería suministrada respaldan la corrección del
    modelo, el conversor y la comprobación independiente de restricciones.
\end{enumerate}

\section{Video explicativo}

\pendiente{reemplazar este texto por el enlace público o institucional al
video de máximo 15 minutos antes de entregar.}

\clearpage
\appendix
\begin{landscape}
\section{Resultados completos de la batería suministrada}

La tabla~\ref{tab:bateria-completa} presenta las 30 entradas suministradas,
ejecutadas con MiniZinc 2.9.7 y COIN-BC 2.10.13. La columna \emph{Ref.}
corresponde al valor publicado por el profesor y \(\Delta\) es la diferencia
absoluta frente al resultado del modelo. Todas las ejecuciones terminaron con
estado óptimo y aprobaron la comprobación independiente de dominio, balance,
conservación, presupuesto, movimientos y polarización. Los tiempos son
mediciones puntuales del solver.

\scriptsize
\setlength{\tabcolsep}{3.2pt}
\renewcommand{\arraystretch}{1.12}
\begin{longtable}{lrrrrrrrrrrr}
\caption{Resultados individuales de las 30 instancias suministradas.}
\label{tab:bateria-completa}\\
\toprule
Instancia & \(n\) & \(m\) & \(ct\) & \(maxM\) & Costo & Movs. & Pol. & Ref. & \(\Delta\) & Tiempo (s) & Nodos\\
\midrule
\endfirsthead
\multicolumn{12}{c}{\tablename\ \thetable{} -- continuación}\\
\toprule
Instancia & \(n\) & \(m\) & \(ct\) & \(maxM\) & Costo & Movs. & Pol. & Ref. & \(\Delta\) & Tiempo (s) & Nodos\\
\midrule
\endhead
\midrule
\multicolumn{12}{r}{Continúa en la página siguiente.}\\
\endfoot
\bottomrule
\endlastfoot
MinPol1 & 5 & 5 & 16.38 & 5 & 6.5988 & 5 & 0.09 & 0.09 & 0 & 0.0545 & 0\\
MinPol2 & 5 & 5 & 75.43 & 39 & 15.6168 & 11 & 0 & 0 & 0 & 0.1365 & 0\\
MinPol3 & 5 & 10 & 128.46 & 140 & 12.1116 & 11 & 0 & 0 & 0 & 0.0113 & 0\\
MinPol4 & 10 & 5 & 38.74 & 19 & 23.8851 & 11 & 0 & 0 & 0 & 0.0052 & 0\\
MinPol5 & 10 & 5 & 29.35 & 9 & 21.2271 & 9 & 0.482 & 0.482 & 0 & 0.1989 & 10\\
MinPol6 & 20 & 5 & 22.3 & 26 & 20.5868 & 22 & 0.003 & 0.003 & 0 & 0.1744 & 6\\
MinPol7 & 35 & 5 & 8.44 & 20 & 7.939628571 & 11 & 3.085 & 3.085 & 0 & 0.2598 & 8\\
MinPol8 & 35 & 5 & 3.43 & 16 & 2.908571429 & 12 & 4.323 & 4.323 & 0 & 0.3304 & 6\\
MinPol9 & 50 & 5 & 15.37 & 7 & 7.94814 & 7 & 5.373 & 5.373 & 0 & 0.2657 & 2\\
MinPol10 & 50 & 5 & 5.85 & 18 & 5.59266 & 5 & 9.687 & 9.686 & 0.001 & 0.1948 & 0\\
MinPol11 & 50 & 10 & 38.02 & 25 & 25.93028 & 25 & 10.349 & 10.349 & 0 & 0.7204 & 20\\
MinPol12 & 50 & 10 & 18.78 & 42 & 18.77594 & 42 & 3.721 & 3.721 & 0 & 0.5436 & 36\\
MinPol13 & 75 & 10 & 68.39 & 69 & 68.28957333 & 69 & 12.417 & 12.417 & 0 & 0.4184 & 13\\
MinPol14 & 75 & 10 & 26.25 & 71 & 26.22126667 & 71 & 6.55 & 6.549 & 0.001 & 0.6211 & 40\\
MinPol15 & 100 & 5 & 52.42 & 35 & 47.67069 & 35 & 10.313 & 10.313 & 0 & 0.1705 & 8\\
MinPol16 & 100 & 5 & 5.82 & 38 & 2.7312 & 38 & 18.786 & 18.786 & 0 & 0.3597 & 8\\
MinPol17 & 100 & 10 & 5.05 & 43 & 4.81794 & 43 & 14.172 & 14.171 & 0.001 & 1.7040 & 184\\
MinPol18 & 100 & 10 & 8.31 & 42 & 6.16068 & 42 & 17.898 & 17.897 & 0.001 & 0.6343 & 105\\
MinPol19 & 100 & 20 & 22.44 & 14 & 13.92211 & 14 & 19.603 & 19.603 & 0 & 0.5318 & 18\\
MinPol20 & 100 & 20 & 1.67 & 4 & 1.62614 & 4 & 23.567 & 23.566 & 0.001 & 4.9216 & 110\\
MinPol21 & 120 & 3 & 38.76 & 44 & 32.66826667 & 44 & 24.176 & 24.175 & 0.001 & 0.0648 & 0\\
MinPol22 & 150 & 3 & 18.37 & 9 & 12.80982 & 9 & 31.135 & 31.135 & 0 & 0.0112 & 0\\
MinPol23 & 150 & 3 & 36.29 & 17 & 7.48972 & 17 & 3.822 & 3.822 & 0 & 0.0068 & 0\\
MinPol24 & 200 & 3 & 3.82 & 9 & 2.26497 & 1 & 29.902 & 29.902 & 0 & 0.0040 & 0\\
MinPol25 & 180 & 3 & 15.37 & 25 & 15.02111111 & 20 & 24.775 & 24.775 & 0 & 0.0072 & 0\\
MinPol26 & 100 & 5 & 80.76 & 20 & 70.025 & 20 & 30.748 & 30.748 & 0 & 0.1569 & 8\\
MinPol27 & 100 & 5 & 63.68 & 30 & 63.049 & 30 & 25.021 & 25.021 & 0 & 0.1693 & 12\\
MinPol28 & 125 & 5 & 40.44 & 31 & 35.4204 & 31 & 14.683 & 14.683 & 0 & 0.1705 & 8\\
MinPol29 & 125 & 5 & 60.02 & 30 & 33.15 & 30 & 30 & 29.999 & 0.001 & 0.1803 & 8\\
MinPol30 & 100 & 5 & 1000.0 & 1000 & 184.575 & 200 & 0 & 0 & 0 & 0.0258 & 0\\
\end{longtable}
\end{landscape}

\end{document}
